Trusted-side human-case oracles for the Customer360 benchmark.

This directory is committed in the public source tree so developers can
run coverage/Gold/rewrite checks for C360_0001–0020. It is not the hidden
set (C360_4001+). Do not copy human_oracles.yaml into src/customer360,
wheel, sdist, Docker images, Agent inputs, or public reports.
Public questions and rewrites live in the packaged human_cases.yaml.
