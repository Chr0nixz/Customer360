# AGENTS.md

本文件是 Customer360 Agent Benchmark 的开发协作约定，适用于在本仓库中工作的所有 Agent、开发者和自动化任务。

## 1. 项目目标

本项目不是普通的问数 Demo，而是一套可复现、可审计的 Agent benchmark。优先级依次是：

1. 评测可信；
2. 业务语义明确；
3. 安全默认开启；
4. 结果可复现；
5. 接入和扩展简单；
6. 最后才是题量、模型效果和性能优化。

完整阶段计划见 [ROADMAP.md](./ROADMAP.md)，原始设计依据见 [Customer360-Agent-Benchmark-Implementation-Plan.md](./Customer360-Agent-Benchmark-Implementation-Plan.md)。怎么跑 CLI 见 [使用说明](docs/user-guide.md)。公开到 GitHub 前见 [GitHub 发布清单](docs/github-publish.md) 和 [CONTRIBUTING.md](CONTRIBUTING.md)。开始工作前先阅读这些文件，并确认当前任务属于哪个里程碑。

本文件描述目标约定和当前可信边界，不代替源码/测试验收。当前仓库已包含 M0–M7 的实现、正式评分和 formal release 工具链，但 v1.0 标签仍需完成所有 RC evidence gates 后才可打出；未通过的门不能用 manifest 字段伪装成通过。原方案中的矛盾与补充约定以 ROADMAP 的明确说明和后续经确认的决策记录为准，未决项不可宣称已冻结。

## 2. 工作边界

### 默认技术基线

- Python 3.11；
- DuckDB 为默认执行引擎；
- SQLGlot 负责 SQL AST 解析和静态检查；
- Pydantic 负责跨模块数据契约；
- Pytest 负责测试；
- Typer 负责 CLI；
- FastAPI 负责可选 HTTP 接口；
- YAML 负责配置和大部分业务元数据；
- JSONL/Parquet 负责实验记录和批量结果。

如果需要替换技术组件，必须说明：替换原因、兼容影响、迁移方式和测试覆盖；不能只在一个模块中悄悄引入第二套方案。

### 允许做的事

- 在任务范围内新增实现、测试、配置、文档和样例；
- 为了验证行为运行本地生成器、DuckDB 查询和 evaluator；
- 在不泄漏隐藏集的前提下补充公开样例和回归 fixture；
- 对已有设计提出问题，但先用不改变语义的默认值推进工作。

### 不允许默认做的事

- 不接入、复制或提交真实客户数据、生产凭证、真实手机号、真实身份证号等敏感信息；安全 fixture 只能使用明确标记的合成值；
- 不把 LLM 直接当作 Gold SQL、Gold Answer 或最终裁判；
- 不关闭或绕过 SQL Guard、权限检查、结果脱敏和行数限制；
- 不为“先跑起来”修改评分规则、隐藏集或业务指标口径；
- 不引入未记录的全局随机性、网络依赖或本机绝对路径；
- 不为了扩大范围提前实现 PostgreSQL、多模型编排或营销扩展表，除非任务明确要求。

## 3. 开始任务前

1. 阅读本文件、ROADMAP.md 和原始方案；
2. 检查工作区现状和已有改动，不覆盖他人未提交的修改；
3. 确定目标模块、输入输出契约、验收标准和影响范围；
4. 搜索现有实现和测试，优先复用而不是平行造轮子；
5. 若发现方向性歧义，先在任务说明或变更记录中写出假设；只有会改变架构、评分或公开范围的歧义才需要阻塞询问。

## 4. 推荐开发流程

按以下顺序推进：

1. 先写/更新数据模型、协议或业务规则；
2. 再写失败测试或最小回归 fixture；
3. 实现最小正确路径；
4. 运行目标模块测试，再运行 Tiny 全量回归；
5. 检查数据摘要、任务版本、评测记录和日志是否稳定；
6. 更新文档、CLI 帮助和变更说明；
7. 最终报告应包含改动、验证命令、未覆盖风险和需要确认的决策。

不要把“大量题目生成成功”当作完成标准；先确认生成的 semantic spec、参考查询和结果都正确。

## 5. 架构边界

推荐目录职责如下：

~~~
src/customer360/synth       数据生成、分布、约束、快照
src/customer360/metadata    元数据模型、仓储、检索、工具
src/customer360/tasks       semantic DSL、模板、生成、改写、校验
src/customer360/agent       Agent 协议、规划、SQL、澄清、baseline
src/customer360/safety      SQL、权限、结果安全
src/customer360/evaluator   执行、语义比较、交互、安全、延迟、报告
src/customer360/cli.py      CLI 入口
tests                        单元、集成、回归和对抗测试
data                         Schema、元数据、公开/隐藏数据、任务
configs                      benchmark、生成、评测和安全配置
docs                         架构、字典、任务、评测和接入文档
~~~

模块依赖约束：

- "synth" 不依赖 "agent" 或 "evaluator"；
- "metadata" 不执行任意 Agent SQL；
- "tasks" 通过结构化 DSL 生成 Gold，不调用 Agent 产生 Gold；
- "agent" 只能通过公开工具/协议访问元数据和执行能力；
- "evaluator" 是最终裁判，不能调用被评测 Agent 的内部判断作为真值；
- "safety" 必须在执行前和结果输出前各有检查点。

跨模块协议应放入轻量共享契约层，不能通过导入 baseline 内部类形成循环依赖。Gold 编译、私有任务/答案和安全执行网关属于可信侧；参评 Agent 只得到问题、授权元数据和受控工具，不得读取 semantic spec、缺失 slot 标注、Gold、隐藏种子或评分脚本内部状态。

## 6. 数据与版本规则

通过 manifest 保存和引用各层适用版本，至少覆盖：

~~~
seed
snapshot_version
generator_version
metadata_version
task_version
protocol_version
~~~

同时记录生成配置摘要、schema/评分/evaluator 版本及锁定的依赖环境；运行记录额外包含 Agent/模型版本、模型参数、硬件、并发、缓存和重试策略。数据生成器不必伪填尚不存在的 task 版本。数据内容可复现与外部模型输出可复现是不同保证，不能声称仅靠 seed 能锁定外部 API 行为。

### 合成数据

- 默认 seed 为 42，但代码必须支持显式传入；
- 同一版本和 seed 必须生成相同数据及稳定摘要；
- 摘要以主键稳定排序后的规范化数据为准，文件字节一致性只有在格式/依赖版本固定时才单独承诺；禁止用系统当前时间或进程随机 hash 决定业务值；
- 业务约束优先于随机分布；
- 金额使用固定精度 Decimal/DECIMAL 或最小货币单位；相加前校验币种，不对跨币种金额直接求和；
- 日期比较使用任务 anchor date，明确窗口端点、时区和日/月/季度单位；年龄由出生日期和指定日期计算，不能假设原始表有 age 字段；
- Tiny 是默认开发和 PR 回归集；Standard/Large 用于集成和性能验证；
- 新增字段、表或规则要同步更新 DDL、元数据、质量检查、样例和文档。

### Gold 和任务

- 先生成 semantic spec，再编译参考 SQL，再执行得到参考结果，最后生成自然语言；
- 自然语言改写必须通过语义一致性检查；
- 每道题记录预期 action：answer、clarification_needed 或 refuse；
- answer 题的 Gold 是程序生成的查询/结果；澄清题保存缺失 slot、脚本化回复以及补齐后的语义；拒答题保存 reason code，不编造可执行 Gold SQL；
- DSL 不支持的业务构造应明确报错并补契约，不能静默丢条件；人工可算 fixture 或独立计算逻辑必须交叉检查编译结果；
- 时间窗口、指标口径、粒度、空值规则和去重规则不能隐含在 prompt 中；
- “近 90 天”与“最近三个月”不互换；join 后如何去重由目标粒度决定，不把所有问题机械改为 COUNT DISTINCT；
- 公开集和隐藏集按模板、指标组合、Join 路径、时间表达和数据分布隔离；
- 20/120/300 按独立语义 case 计数；同题改写、数据变体不重复计题，同源语义与改写不得跨 split；
- 修改 Gold 必须说明原因并更新 task/data 版本，不能只改期望值。

## 7. Agent 协议约束

工具调用统一使用可序列化 JSON：

~~~json
{
  "tool": "search_metrics",
  "arguments": {"query": "客户资产规模"}
}
~~~

最终输出至少遵守以下状态约定：

- "success"：包含答案、SQL、列说明、假设、证据、置信度，以及受控执行器返回的结构化结果或可追溯 query_id；不能只凭答案文字判对；
- "clarification_needed"：只提出最少但足够的问题，不擅自补齐关键条件；
- "refused"：说明安全/权限原因，并尽可能给出合规替代方案；
- "error"：记录可诊断错误，不伪装成成功。

expected action 与输出状态的映射为 answer → success、clarification_needed → clarification_needed、refuse → refused。reason code 区分权限拒绝、未知字段/指标、政策禁止和运行故障；不能把所有非 success 都算成正确拒答。澄清回放使用隐藏的确定性 slot 脚本，比较请求槽位、轮次和最终结果，不要求与标准问句逐字相同。

Agent 不得假设：

- “最近”默认等于某个固定天数；
- “资产”“客户数”“活跃”等业务词自动对应某个指标；
- 多表 Join 后可以直接 COUNT(*)；
- 失败、撤销交易可直接纳入业务指标；
- 缺少权限时可以通过换字段、换表或自然语言改写绕过策略。

## 8. 安全规则

安全是默认行为，不是可选插件。

### SQL 层

- 只允许单条只读查询；WITH 是语法结构而不是安全凭证，必须检查全部 AST、CTE、子查询和函数调用；
- 拒绝 INSERT、UPDATE、DELETE、DROP 等修改性语句；
- 禁止非白名单文件/网络读取、扩展加载、数据库挂载、系统表访问和具有副作用的函数；只读连接不替代进程/文件/网络隔离；
- 校验表、列、Join 路径、笛卡尔积、重复计数风险；
- 限制返回行数、扫描量和执行时长；
- 禁止直接输出敏感字段。

执行网关默认拒绝未知能力和策略错误；AST 只负责能静态证明的部分，Join 语义错误仍由 evaluator 判定。超时要真正终止/回收执行，不能只让调用方停止等待；结果截断必须带标记，不能把截断结果当作完整正确答案。不支持的扫描量等统计必须标为不可用，不可伪报 0 或声称已强制限制。

### 权限层

- 同时检查角色、允许表、允许列和客户范围；
- 角色来自可信测试上下文而非用户提示词；客户行级范围必须在聚合前由可信侧实施，不能依靠 Agent 自觉补 WHERE 或仅在结果阶段过滤；
- 不把“查不到”与“无权限”混为一谈；
- 发生权限拒绝时保留可审计原因，但不泄漏被保护对象的存在性细节。

### 结果层

- SQL 合法不代表结果安全；
- 检查敏感字段、明细规模、最小聚合粒度、个人可识别信息和越权范围；
- 拒答、脱敏、聚合或请求提权必须与任务的预期 action 一致。

任何新增数据出口、调试日志或报告字段都要先检查是否可能泄漏隐藏集或敏感信息。

## 9. 评测正确性规则

- 最终正确性来自结构化语义、参考查询和执行结果；
- 不比较 SQL 字符串；
- 默认无序结果按多重集比较，保留重复行数量；只有 DSL 明确集合语义时才去重；有序/Top-K 题必须定义并列处理；
- 比较器必须处理数值绝对/相对容差、日期归一化、NULL、小数精度和空结果；NULL、0、空字符串不可混同，整数计数必须精确；
- 正式可回答题至少有正常数据 + 4 个变体，默认所有适用变体通过才算语义正确；单个变体得分可另报诊断信息；
- 数据变体默认重放同一 SQL/查询计划以检查语义；重新调用完整 Agent、元数据漂移后的重新规划属于不同评测模式，分别记录；
- 各类已知错误 SQL 要有能区分它与 Gold 的 fixture/变体，避免空结果或过于简单的数据使错误 SQL 偶然通过；
- LLM Judge 只能辅助解释、失败分类和人工复核排序；
- 失败必须尽可能归入稳定分类，例如 JOIN_ERROR、TIME_RANGE_ERROR、DUPLICATE_COUNT_ERROR、UNSAFE_SQL 或 CLARIFICATION_FAILURE；
- evaluator 的异常、超时和执行失败不能静默计为成功或空结果。

同时记录“Agent 提交了违规请求”和“执行网关是否拦截成功”；不能因为网关兜底就给 Agent 安全项满分。原始私有报告只能由维护者访问，对外摘要不得包含 Gold、隐藏结果、种子或能还原私有用例的细节。

## 10. 测试要求

每类改动至少考虑以下测试：

- 单元测试：模型校验、规则、解析和比较器边界；
- 集成测试：DuckDB 加载、元数据工具、任务编译、Agent 协议；
- 回归测试：20 道人工题和固定 seed 摘要；
- 对抗测试：危险 SQL、越权字段、笛卡尔积、重复计数、空值和边界日期；
- 可复现性测试：同 seed 重跑输出一致，不同版本能被识别；
- 性能测试：Tiny 日常回归，Standard/Large 只在指定流水线运行。

提交前优先运行：

~~~bash
pytest -q
c360 generate-data --scale tiny --seed 42
c360 evaluate --split dev --agent baseline
~~~

如果命令尚未实现，应运行对应模块测试，并在交付说明中明确缺失项；不要伪造成功输出。

## 11. CLI 与配置约定

推荐命令：

~~~bash
c360 generate-data --scale tiny --seed 42
c360 build-metadata-index
c360 generate-tasks --count 300 --seed 42
c360 run-case --case-id C360_0001 --agent baseline
c360 evaluate --split dev --agent baseline
c360 report --input outputs/run.jsonl --format html
~~~

- 命令必须支持 --help；
- 路径、规模、seed、split、agent、输出格式优先使用参数或配置，不写死；
- 配置文件中的路径应相对仓库或显式传入；
- 输出目录与源数据、隐藏数据分离；
- 机器可读结果写 JSONL/Parquet，人读摘要写 HTML/Markdown。

## 12. 文档与变更记录

涉及以下内容时必须更新文档：

- 指标、Join、权限或字段口径；
- semantic DSL 或 Agent 协议；
- 评分权重、硬门槛或失败分类；
- CLI 参数、配置字段或目录结构；
- 数据生成规则、seed、版本和切分策略；
- GitHub 公开范围、社区文件或打包白名单。

文档变更应回答三件事：改了什么、为什么改、如何验证。若仍有产品/赛制不确定性，记录在 ROADMAP.md 第 10 节或单独的决策记录中。

评分权重和硬门槛以 ROADMAP.md 第 8 节为准。修改权重、失败分类或硬门槛必须视为 benchmark 协议变更，更新版本、回归基线和发布说明。

## 13. 完成定义

一个任务只有同时满足以下条件才算完成：

- 实现符合模块边界和版本规则；
- 有针对性测试，且 Tiny 回归通过；
- 没有关闭安全检查或绕过 evaluator；
- 结果、日志和报告可审计；
- 相关 CLI、协议、配置和文档已同步；
- 交付说明列出验证命令、已知限制和需要用户决策的问题。

发现会改变整体方向的问题时，应立即停在当前边界并提问；发现只是实现细节不确定时，采用与本文件一致的最小假设继续推进，并把假设写入代码或文档。
